<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activity.MainActivity"
    android:background="@drawable/bg">

    <ImageView android:id="@+id/am_iv_logo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:src="@drawable/logo"/>

    <EditText android:id="@+id/am_et_mobile_no"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/am_iv_logo"
        android:background="@drawable/et_bg"
        android:layout_marginTop="24dp"
        android:layout_marginLeft="32dp"
        android:layout_marginRight="32dp"
        android:layout_marginBottom="12dp"
        android:padding="16dp"
        android:hint="Login Name"
        android:textColorHint="#000000"
        android:maxLines="1"
        android:inputType="text"
        android:drawableLeft="@drawable/user"
        android:drawablePadding="12dp"
        android:textSize="20sp"/>

    <EditText android:id="@+id/am_et_pwd"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/am_et_mobile_no"
        android:background="@drawable/et_bg"
        android:layout_marginTop="32dp"
        android:layout_marginLeft="32dp"
        android:layout_marginRight="32dp"
        android:layout_marginBottom="32dp"
        android:padding="16dp"
        android:hint="Password"
        android:textColorHint="#000000"
        android:maxLines="1"
        android:inputType="textPassword"
        android:drawableLeft="@drawable/pwd"
        android:drawablePadding="12dp"
        android:textSize="20sp"/>

    <Button android:id="@+id/am_bt_login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/am_et_pwd"
        android:background="@drawable/bt_bg"
        android:text="Login"
        android:layout_centerInParent="true"
        android:layout_marginTop="40dp"
        android:layout_marginBottom="12dp"
        android:textAllCaps="false"
        android:textSize="20sp"
        android:padding="12dp"
        />

    <TextView android:id="@+id/am_tv_fpwd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Forgot password?"
        android:textSize="18sp"
        android:layout_centerInParent="true"
        android:textColor="@color/colorPrimary"
        android:layout_margin="32dp"
        android:layout_below="@+id/am_bt_login"/>




</RelativeLayout>
